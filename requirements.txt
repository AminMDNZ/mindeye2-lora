# Core (torch is preinstalled on Colab; do not let anything downgrade it)
numpy>=1.26
scipy>=1.11
h5py>=3.10
fsspec[http]>=2024.2.0
aiohttp>=3.9
huggingface_hub>=0.23
pyyaml>=6.0
matplotlib>=3.8
scikit-image>=0.22
tqdm>=4.66

# CLIP image tower (ViT-bigG/14) used to build the regression targets
open_clip_torch>=2.24

# Required by MindEyeV2's models.py. Installed --no-deps by setup/colab_setup.sh so it
# cannot pull an incompatible torch; its runtime imports are listed there.
# dalle2-pytorch==1.15.6

# Optional, only for the SDXL unCLIP decoder stage
# omegaconf>=2.3
# einops>=0.7
# kornia>=0.7
